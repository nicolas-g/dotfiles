

# Kill process pattern
function killnamed () {
    ps ax | grep $1 | cut -d ' ' -f 2 | xargs kill
}

# Setup a tunnel
function tunnelssh() {
  # ssh -N -L 8090:<target-host>:8080 <ops>
  # localhost:8090
  ssh -L 7997:127.0.0.1:7997 $1
}
